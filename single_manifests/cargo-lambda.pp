
# # As yet, no effort has been made to actually puppetize these steps
# python3 -m venv --prompt venv-cargo-lambda ~/source/venv-cargo-lambda
# source ~/source/venv-cargo-lambda/bin/activate
# pip install wheel
# pip install --upgrade pip
# pip install cargo-lambda
# deactivate
# ln -s ~/source/venv-cargo-lambda/bin/cargo-lambda ~/bin/cargo-lambda
